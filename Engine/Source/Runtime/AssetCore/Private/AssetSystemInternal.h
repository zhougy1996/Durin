#pragma once

// Transitional aggregate for private helpers that only need AssetCore's internal
// type surface. Implementation files should prefer the narrower state headers.
#include "AssetRuntimeStateInternal.h"
