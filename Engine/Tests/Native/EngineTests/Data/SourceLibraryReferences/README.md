# Source-reference contract fixtures

`UnifiedMountContract.json` defines the current named source-library mount,
dependency, and path-resolution cases exercised by
`SourceLibraryReferenceContractTests.cpp`.

Historical DAST packages and their copied source trees are intentionally not
kept. Repository-authored assets define the supported serialization baseline;
pre-baseline source-reference layouts have no migration path.
